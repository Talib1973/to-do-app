🎉 Todo App with AI Chatbot has been deployed to Kubernetes!

Release Name: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

📦 Deployed Components:
{{- if .Values.backend.enabled }}
  ✓ Backend (FastAPI) - {{ .Values.backend.service.name }}:{{ .Values.backend.service.port }}
{{- end }}
{{- if .Values.frontend.enabled }}
  ✓ Frontend (Next.js) - {{ .Values.frontend.service.name }}:{{ .Values.frontend.service.port }}
{{- end }}
{{- if .Values.persistence.enabled }}
  ✓ Persistent Storage - backend-pvc ({{ .Values.persistence.size }})
{{- end }}

🌐 Access Your Application:

{{- if .Values.frontend.enabled }}
{{- if eq .Values.frontend.service.type "NodePort" }}

1. Frontend (Web UI):
   Run: minikube service {{ .Values.frontend.service.name }} -n {{ .Release.Namespace }}

   Or get the URL with:
   export FRONTEND_URL=$(minikube service {{ .Values.frontend.service.name }} -n {{ .Release.Namespace }} --url)
   echo "Frontend URL: $FRONTEND_URL"

{{- end }}
{{- end }}

{{- if .Values.backend.enabled }}

2. Backend API (for testing):
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ .Values.backend.service.name }} {{ .Values.backend.service.port }}:{{ .Values.backend.service.port }}

   Then access: http://localhost:{{ .Values.backend.service.port }}/health

{{- end }}

📊 Monitor Your Deployment:

  Check pod status:
    kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }}"

  View logs:
    Backend:  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=backend --tail=50 -f
    Frontend: kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=frontend --tail=50 -f

  Check services:
    kubectl get svc -n {{ .Release.Namespace }}

🧪 Run Tests:
  helm test {{ .Release.Name }} -n {{ .Release.Namespace }}

📝 Next Steps:
  1. Access the frontend URL in your browser
  2. Sign up or log in to the application
  3. Start managing your tasks with AI assistance!

💡 Tip: For minikube, use 'minikube service list' to see all available services.

Need help? Check the docs or run 'kubectl describe pod <pod-name>' for troubleshooting.
